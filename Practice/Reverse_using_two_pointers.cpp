#include <iostream>
using std namespace;

void reverseArray(int[] arr){
    int left = 0;
    int right = arr.size() - 1;

    while(left < right){
        [arr[left], arr[right]] = [arr[righ], arr[left]]
        left ++;
        right --;
    }
    return arr;
}


int main (){

    int arr[] = {1,2,3,4,5,6};

    result = reverseArray(arr);

    use for each here

}